// Write a JavaScript function declaration, function expression, and an arrow function. Each function should take two numbers as parameters and return their sum.

//Part (i):

const sum = function (n1, n2) {
    return (n1 + n2);
}
console.log(sum(1, 2));

//Part (ii):

let add = (a, b) => a + b;
console.log(add(3, 4));

//Part (iii):
function sumNum(a, b) {
    return a + b;
}
console.log(sumNum(4, 5));